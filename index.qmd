---
title: "Les jeux vidéos et les tueries dans les écoles américaines"
author: "ABDELLOU--GENESTIER Renaud & LAFARGE Baptiste"
format:
  revealjs:
    theme: simple
    footer: "Analyse Big Data - 2026"
    slide-number: true
    progress: true
    center: true
    transition: fade
    background-transition: fade
    highlight-style: github
    title-slide-attributes:
      data-background-color: "#f8f9fa"
---

## Plan

::: {.incremental}
1. [Présentation des données]{style="color: #2c3e50;"}
2. [Préparation des données]{style="color: #2c3e50;"}
3. [Analyse exploratoire]{style="color: #2c3e50;"}
4. [Modélisation]{style="color: #2c3e50;"}
5. [Conclusion]{style="color: #2c3e50;"}
:::

---

## Introduction {background-color="#f8f9fa"}

- **Recrudescence** des tueries dans les écoles américaines.
- **Mise en cause** fréquente des jeux vidéo (contenu violent).
- **Objectif** : Analyser les données pour vérifier s'il existe une corrélation réelle.

::: {.notes}
L'idée ici est de poser le contexte sociétal avant d'entrer dans les chiffres.
:::

## 1. Présentation des données

::: {.columns}
::: {.column width="50%"}
### 🏫 Tueries
- **Date** de l'événement.
- **Bilan humain** (morts/blessés).
- **Localisation** (école/état).
:::

::: {.column width="50%"}
### 🎮 Jeux Vidéo
- **Titre & Genre**.
- **Plateforme**.
- **Ventes Américaines**.
- **Date de sortie**.
:::
:::

## 2. Préparation des données

::: {.callout-note appearance="simple"}
## Processus de traitement
1. **Nettoyage** : Enlever les données inutiles pour alléger la base de données.
2. **Filtrage** : Focus exclusif sur les **États-Unis**.
3. **Jointure** : Fusion temporelle par année.
:::

## 3. Analyse exploratoire

### Évolution des tueries (USA)

```{python}
#| echo: false
#| fig-align: center
import pandas as pd
import sqlite3
import matplotlib.pyplot as plt

plt.style.use('ggplot') 
conn = sqlite3.connect('./data.db')
query = "SELECT year, COUNT(*) AS shooting_count FROM school_shootings WHERE year IS NOT NULL GROUP BY year ORDER BY year ASC"
df = pd.read_sql_query(query, conn)
conn.close()

plt.figure(figsize=(10, 4))
plt.bar(df['year'].astype(str), df['shooting_count'], color='#e67e22', alpha=0.8)
plt.title('Nombre de tueries par année', fontsize=14)
plt.xticks(rotation=45, fontsize=8)
plt.tight_layout()
plt.show()
```

---

### Records de ventes annuels

```{python}
#| echo: false
#| fig-align: center
import pandas as pd
import sqlite3
import matplotlib.pyplot as plt
import matplotlib.cm as cm

conn = sqlite3.connect('./data.db')
query = """
SELECT title, CAST(strftime('%Y', release_date) AS INTEGER) AS year, total_sales 
FROM video_games 
WHERE release_date IS NOT NULL AND total_sales IS NOT NULL
"""
df = pd.read_sql_query(query, conn)
conn.close()

# Préparation des données : Top 1 par an entre 1999 et 2022 (exclusion 2023/2024)
df = df[(df['year'] >= 1999) & (df['year'] <= 2022)]
top_sales = df.sort_values(['year', 'total_sales'], ascending=[True, False]).groupby('year').head(1)

plt.figure(figsize=(10, 7))

# Création d'un dégradé de couleur basé sur les ventes
norm = plt.Normalize(top_sales['total_sales'].min(), top_sales['total_sales'].max())
colors = cm.get_cmap('YlOrRd')(norm(top_sales['total_sales'])) # Dégradé Jaune à Rouge

bars = plt.barh(top_sales['year'].astype(str), top_sales['total_sales'], color=colors, edgecolor='grey', alpha=0.8)

# Ajout des noms des jeux sur les barres
for i, bar in enumerate(bars):
    width = bar.get_width()
    game_title = top_sales.iloc[i]['title']
    plt.text(width + 0.5, bar.get_y() + bar.get_height()/2, 
             f"{game_title} ({width:.1f}M)", 
             va='center', fontsize=7, color='#2c3e50', fontweight='bold')

plt.title('Jeux les plus vendus par année (1999-2022)', fontsize=14)
plt.xlabel('Ventes mondiales (Millions d\'unités)', fontsize=10)
plt.gca().invert_yaxis() # 1999 en haut
plt.xticks(fontsize=8)
plt.yticks(fontsize=8)
plt.grid(axis='x', linestyle='--', alpha=0.3)
plt.tight_layout()
plt.show()
```

---

### Comparaison : Ventes vs Tueries

```{python}
#| echo: false
#| fig-align: center
import pandas as pd
import sqlite3
import matplotlib.pyplot as plt

conn = sqlite3.connect('./data.db')
# Extraction des ventes records
query_vg = "SELECT CAST(strftime('%Y', release_date) AS INTEGER) AS year, MAX(total_sales) AS top_game_sales FROM video_games WHERE release_date IS NOT NULL AND total_sales IS NOT NULL GROUP BY year"
df_vg = pd.read_sql_query(query_vg, conn)
# Extraction des tueries
query_ss = "SELECT year, COUNT(*) AS shooting_count FROM school_shootings WHERE year IS NOT NULL GROUP BY year"
df_ss = pd.read_sql_query(query_ss, conn)
conn.close()

# Fusion et filtrage
df_total = pd.merge(df_vg, df_ss, on='year')
df_total = df_total[(df_total['year'] >= 1999) & (df_total['year'] <= 2022)]

fig, ax1 = plt.subplots(figsize=(10, 5))

# Axe 1 : Ventes (Bleu)
ax1.set_xlabel('Année')
ax1.set_ylabel('Ventes Record (Millions)', color='#3498db')
ax1.plot(df_total['year'], df_total['top_game_sales'], color='#3498db', marker='o', label='Ventes')
ax1.tick_params(axis='y', labelcolor='#3498db')

# Axe 2 : Tueries (Orange)
ax2 = ax1.twinx()
ax2.set_ylabel('Nombre de tueries', color='#e67e22')
ax2.plot(df_total['year'], df_total['shooting_count'], color='#e67e22', marker='s', linestyle='--', label='Tueries')
ax2.tick_params(axis='y', labelcolor='#e67e22')

plt.title('Ventes de Jeux Records vs Tueries Scolaires (1999-2022)')
plt.tight_layout()
plt.show()
```

## 4. Modélisation

- **Analyse de corrélation** : Intensité de la relation statistique.
- **Régression linéaire** : Modèle prédictif (Tueries ~ Ventes).

> Le volume de ventes n'est pas un prédicteur fiable du nombre d'incidents.

## 5. Conclusion {background-color="#2c3e50"}

::: {style="color: white;"}
- **Résultat** : Absence de corrélation directe.
- **Analyse** : Les jeux vidéo ne sont pas le moteur principal.
- **Ouverture** : Facteurs socio-politiques et accès aux armes.
:::
